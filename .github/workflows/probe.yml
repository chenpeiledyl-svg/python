# .github/workflows/schedule-probe.yml
name: Schedule Probe

on:
  schedule:
    - cron: "*/5 * * * *"   # 每5分钟（UTC）
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "UTC: $(date -u)"
          echo "CST: $(TZ=Asia/Shanghai date)"
